# Kết quả build và cache — sinh lại được, không commit.
build/
.gradle/
.idea/
*.iml

# Đường dẫn Android SDK riêng của từng máy (vendor để sdk.dir Windows).
local.properties

# Khoá ký APK — không bao giờ commit.
*.jks
*.keystore
signing.properties
